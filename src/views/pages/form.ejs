<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario</title>
    <link rel="stylesheet" href="../css/app.css">
</head>

<body>
    <h1>
        <%= data[1] %>
    </h1>

    <div class="container">

        <h3>
            Estado:
            <% if ( data[5]==null) { %>
                Por aprobar
                <% } else { %>
                    Aprobado
                    <% } %>
        </h3>
        <input type="hidden" name="form-id" id="form-id" value="<%= data[0] %>">
        <input type="hidden" name="form-title" id="form-title" value="<%= data[1] %>">
        <input type="hidden" name="form-version" id="form-version" value="<%= data[2] %>">
        <input type="hidden" name="form-date-sol" id="form-date-sol" value="<%= data[3] %>">
        <input type="hidden" name="form-priority" id="form-priority" value="<%= data[4] %>">
        <input type="hidden" name="form-status" id="form-status" value="<%= data[5] %>">
        <input type="hidden" name="form-description" id="form-description" value="<%= data[6] %>">
        <input type="hidden" name="form-reason" id="form-reason" value="<%= data[7] %>">
        <input type="hidden" name="form-solicitante" id="form-solicitante" value="<%= data[8] %>">
        <input type="hidden" name="form-project" id="form-project" value="<%= data[9] %>">
        <form id="form-accept">
            <fieldset>
                <legend>Autorización</legend>

                <div>
                    <label for="form-date">Fecha:</label>
                    <input type="date" id="form-date" name="form-date"
                        value="<%= data[10] == null ? '' : new Date(data[10]).toISOString().split('T')[0] %>"
                        <%=data[10]==null ? '' : 'disabled' %>>
                </div>

                <div>
                    <label for="form-reviewer">Revisor:</label>
                    <select name="form-reviewer" id="form-reviewer" <%=data[11]==null ? '' : 'disabled' %>>
                        <option value="<%= data[11] || '' %>" disabled selected>
                            <%= data[11] || 'Seleccione al revisor' %>
                        </option>
                    </select>
                </div>

                <div>
                    <label for="form-enviroment">Entorno:</label>
                    <input type="text" id="form-enviroment" name="form-enviroment"
                        placeholder="Pruebas, desarrollo, ..." value="<%= data[12] || '' %>" <%=data[12]==null ? ''
                        : 'disabled' %>>
                </div>
            </fieldset>

            <fieldset>
                <legend>Módulos</legend>

                <div>
                    <label for="form-modules">Modificados</label>
                    <input type="text" id="form-modules" name="form-modules" placeholder="Modulos modificados..."
                        value="<%= data[13] || '' %>" <%=data[13]==null ? '' : 'disabled' %>>
                </div>

                <div>
                    <label for="form-affected">Afectados</label>
                    <input type="text" id="form-affected" name="form-affected"
                        placeholder="Modulos directamente afectados/acoplados" value="<%= data[14] || '' %>"
                        <%=data[14]==null ? '' : 'disabled' %>>
                </div>

            </fieldset>

            <button type="submit">Aceptar</button>
        </form>
        <br>
        <form id="form-delete">
            <button type="submit">Borrar</button>
        </form>
        <a href="/forms">Volver</a>
    </div>
    <script src="../js/form.js"></script>
</body>

</html>